{
  "rules": {
    "admins": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": false
      }
    },
    "chatrooms": {
      "$room": {
        ".read": true,
        ".write": "newData.exists() === false && data.exists() === true && auth != null && root.child('admins').child(auth.uid).exists()",
        "$msg": {
          ".write": "(!data.exists() && newData.exists()) || (data.exists() && !newData.exists() && auth != null && root.child('admins').child(auth.uid).exists())",
          ".validate": "newData.hasChildren(['name','text','ts']) && newData.child('text').isString() && newData.child('text').val().length <= 300 && newData.child('text').val().length > 0 && newData.child('name').isString() && newData.child('name').val().length <= 20",
          "name": { ".validate": "newData.isString()" },
          "text": { ".validate": "newData.isString()" },
          "ts":   { ".validate": "newData.isNumber()" },
          "$other": { ".validate": false }
        }
      }
    },
    "$other": { ".read": false, ".write": false }
  }
}
